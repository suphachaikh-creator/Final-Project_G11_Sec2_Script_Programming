# CHANGELOG

บันทึกการเปลี่ยนแปลงของโปรเจกต์ **HOW_DO_YOU_FISH**

รูปแบบเวอร์ชันอิงตาม [Semantic Versioning](https://semver.org/lang/th/) — `MAJOR.MINOR.PATCH`
โดยแต่ละสปรินต์ของรายวิชาจะปล่อยเป็นเวอร์ชัน `MINOR` หนึ่งรอบ และเวอร์ชัน `1.0.0`
คือการส่งมอบโปรเจกต์ฉบับสมบูรณ์

| เวอร์ชัน | สปรินต์ | สัปดาห์ | วันที่ปล่อย |
|---|---|---|---|
| [0.2.2](#022--เพิ่ม-open-fisheries-api-เป็น-api-ตัวที่สอง) | Sprint 2 (แก้ไขระหว่างสปรินต์) | 13 | ปล่อยแล้ว |
| [0.2.1](#021--เปลี่ยนไปใช้-worms-api) | Sprint 2 (แก้ไขระหว่างสปรินต์) | 13 | ปล่อยแล้ว |
| [0.2.0](#020--sprint-2) | Sprint 2 — Back-End App Dev | 13 | ปล่อยแล้ว |
| [0.1.0](#010--sprint-1) | Sprint 1 — Front-End App Dev | 12 | ปล่อยแล้ว |

> แผนของเวอร์ชันถัดไป (v0.3.0 และ v1.0.0) อยู่ใน [`PLAN.md`](PLAN.md)
> เอกสารนี้บันทึกเฉพาะสิ่งที่ทำเสร็จและปล่อยออกไปแล้วเท่านั้น

---

## [0.2.2] — เพิ่ม Open Fisheries API เป็น API ตัวที่สอง

**Sprint 2 (แก้ไขระหว่างสปรินต์)** · สัปดาห์ที่ 13 · *ปล่อยแล้ว*

### เพิ่มเข้ามา
- **`src/openfisheries_api.py`** — คลาส `OpenFisheriesAPI` เรียก
  `GET /api/landings/species.json` เพื่อดึง **ชื่อสามัญภาษาอังกฤษ** ของสัตว์น้ำ
  ทดสอบจริงได้ 11,562 รายการ สร้างเป็นตารางจับคู่ ชื่อวิทยาศาสตร์ → ชื่อสามัญ **8,672 คู่**
- **`src/worms_api.py`** — แยกตัวเชื่อมต่อ WoRMS ออกมาเป็นไฟล์ของตัวเอง
  แต่ละ API จึงมีคลาสของตัวเองหนึ่งไฟล์ ตามหลัก Separation of Concerns
- ชุดทดสอบใหม่ `tests/test_worms_api.py` (32 เคส) และ
  `tests/test_openfisheries_api.py` (10 เคส)

### เปลี่ยนแปลง
- **`src/fish_api.py` เปลี่ยนบทบาทเป็นตัวประสาน** ไม่ได้ยิง HTTP เองอีกต่อไป
  แต่รวมผลจาก `WormsAPI` เข้ากับ `OpenFisheriesAPI` โดยจับคู่ด้วย **ชื่อวิทยาศาสตร์**
  ผู้เล่นจึงเห็นชื่ออย่าง `Atlantic salmon` แทน `Salmo salar`
- `tools/seed_fish_data.py` ดึงจากทั้งสอง API และเรียงให้ชนิดที่มีชื่อสามัญขึ้นก่อน
  ก่อนตัดตาม `--limit` ผลคือ `data/fish_species.json` **มีชื่อสามัญครบ 100%**
  (เดิมเรียงตามตัวอักษรเฉยๆ จึงได้แค่ 20%)
- `tests/test_fish_api.py` เขียนใหม่เป็น 16 เคส เน้นการรวมข้อมูลสองแหล่งและระบบสำรอง
  รวมทั้งโปรเจกต์เป็น **135 เคส**

### ข้อกำหนดการออกแบบ
- ทั้งสอง API มีระดับความสำคัญต่างกัน — **WoRMS เป็นข้อมูลหลัก** ถ้าล้มเหลวจะสลับไปใช้
  ข้อมูลสำรองในเครื่อง ส่วน **Open Fisheries เป็นข้อมูลเสริม** ถ้าล้มเหลวเกมยังเล่นได้ตามปกติ
  เพียงแต่แสดงชื่อวิทยาศาสตร์แทน โดย `fetch_common_names()` คืน dict ว่างและไม่โยน exception

---

## [0.2.1] — เปลี่ยนไปใช้ WoRMS API

**Sprint 2 (แก้ไขระหว่างสปรินต์)** · สัปดาห์ที่ 13 · *ปล่อยแล้ว*

### เปลี่ยนแปลง
- **เปลี่ยนแหล่งข้อมูลหลักเป็น WoRMS API** (World Register of Marine Species)
  แทนการพัฒนา Mock API ขึ้นเอง จึงเป็นการใช้ public API จริงตามข้อกำหนดของรายวิชา
- เพิ่มแหล่งน้ำที่สามคือ **ปากแม่น้ำน้ำกร่อย (brackish)** ตามที่ WoRMS แบ่งไว้
  เดิมมีแค่น้ำจืดกับน้ำเค็ม
- `src/fish_api.py` เขียนใหม่ทั้งไฟล์ — ค้นด้วย `AphiaRecordsByVernacular`
  กรอง 3 ชั้น (เป็นปลาจริง · สถานะ accepted · ตรงแหล่งน้ำ) และแคชผลต่อหนึ่งแหล่งน้ำ
- `data/fish_species.json` สร้างใหม่จาก WoRMS — 90 รายการ (แหล่งน้ำละ 30)
- `tools/seed_fish_data.py` เขียนใหม่ให้ดึงจาก WoRMS
- ชุดทดสอบ `test_fish_api.py` เขียนใหม่เป็น 26 เคส รวมทั้งโปรเจกต์เป็น 114 เคส

### ลบออก
- `api_server.py` — Mock API ที่พัฒนาเอง ไม่ได้ใช้แล้ว
- `FishAPI.report_catch()` และ endpoint `POST /catches` · `GET /stats`
  เพราะ WoRMS เป็น API แบบอ่านอย่างเดียว ส่วนสถิติย้ายไปคำนวณจาก `GameState.summary()` แทน

### แก้ไข
- **เลือก endpoint ผิดตัว** — โค้ดอ้างอิงที่ได้รับมาใช้ `AphiaRecordsByName` เป็นตัวหลัก
  ซึ่งค้นจากชื่อวิทยาศาสตร์ การค้นคำอย่าง `fish` หรือ `shark` จึงไปตรงกับสกุลของสัตว์กลุ่มอื่น
  เช่น *Fisherana* ที่เป็นหนอนทะเล **ทดสอบแล้วได้ปลากลับมา 0 จาก 115 รายการ**
  แก้โดยเปลี่ยนไปใช้ `AphiaRecordsByVernacular` ซึ่งค้นเจอปลาจริง 650 ชนิด
- รองรับ HTTP `204` ที่ WoRMS ใช้บอกว่าค้นไม่เจอ โดยถือเป็นผลลัพธ์ว่าง ไม่ใช่ข้อผิดพลาด

---

## [0.2.0] — Sprint 2

**Back-End App Dev** · สัปดาห์ที่ 13 · *ปล่อยแล้ว*

### เพิ่มเข้ามา
- `src/fish.py` — คลาส `Fish` พร้อมกฎการสุ่มน้ำหนักและคำนวณราคา
- `src/game_state.py` — คลาส `GameState` เก็บเงิน กระเป๋าปลา เลเวลอุปกรณ์ และกฎการซื้อขาย
- `src/minigame.py` — คลาส `QTEMinigame` แยกตรรกะมินิเกมออกมาเป็นอิสระ พร้อมตัวจับเวลาที่ฉีดเข้ามาได้
- `src/fish_api.py` — คลาส `FishAPI` เรียก Fish Mock API ด้วย `requests` ทั้ง GET และ POST
- `src/save_manager.py` — คลาส `SaveManager` บันทึกและโหลด `data/save_game.json`
- `api_server.py` — **Fish Mock API ที่พัฒนาเอง** ด้วย `http.server` มี 4 endpoint
- `data/fish_species.json` — ฐานข้อมูลชนิดปลา 18 ชนิด (น้ำจืด 8 · ทะเล 10)
- `tools/seed_fish_data.py` — สคริปต์ดึงชนิดปลาเพิ่มจาก Open Fisheries API โดยไม่ทับรายการเดิมที่มีคำอธิบายภาษาไทย
- ระบบ **ค้นหา · กรอง · เรียงลำดับ** และสรุปสถิติในหน้าคลังสินค้า
- ชุดทดสอบเพิ่ม 60 เคส (`test_game_state` 24 · `test_minigame` 14 · `test_fish_api` 12 · `test_save_manager` 10)

### เปลี่ยนแปลง
- ย้ายตรรกะทั้งหมดที่เคยรวมอยู่ในคลาส `FishingGame` ออกมาเป็นคลาสตามชั้น ทำให้เขียน unit test ได้
- ย้ายฐานข้อมูลปลาสำรองจาก constant ในโค้ดไปเป็นไฟล์ `data/fish_species.json`
- เรียงลำดับกระเป๋าปลาด้วย `sorted()` แทน `.sort()` เพื่อไม่ให้ลำดับจริงในกระเป๋าถูกเปลี่ยนถาวร

### แก้ไข
- **`FishAPI` เรียก API ไม่สำเร็จทุกครั้ง** — โค้ดเดิมตั้ง `url = ""` แล้วครอบด้วย
  `except Exception: pass` ทำให้ตกไปใช้ข้อมูลสำรองตลอดโดยไม่มีใครรู้
  แก้โดยชี้ไปที่ Fish Mock API ดัก exception เป็นชนิดเจาะจง และเพิ่มธง `using_fallback`
  กับข้อความใน `last_error` ให้ตรวจสอบได้
- ไฟล์เซฟที่เสียหายหรือไม่ใช่ JSON object ไม่ทำให้เกมพังอีกต่อไป แต่เริ่มเกมใหม่แทน

---

## [0.1.0] — Sprint 1

**Front-End App Dev** · สัปดาห์ที่ 12 · *ปล่อยแล้ว*

### เพิ่มเข้ามา
- `main.py` — จุดเริ่มโปรแกรม พร้อมบังคับ UTF-8 ทั้ง stdin และ stdout บน Windows Console
- `src/ui.py` — ส่วนแสดงผลทั้งหมด มีเฉพาะ `print()` ไม่มีตรรกะ
- `src/validators.py` — ตรวจสอบอินพุตทุกช่อง ไม่มี `input()` และ `print()` จึงทดสอบได้ตรง
- `src/cli.py` — การไหลของเมนูทั้ง 5 หน้าจอ
- `tests/test_validators.py` — ชุดทดสอบ 29 เคส
- `.github/workflows/ci.yml` — CI รัน `flake8` และ `pytest` อัตโนมัติทุก push และ pull request
- `PLAN.md` · `CHANGELOG.md` · `reports/` · `.gitignore` · `.flake8`

### แก้ไข
- **บั๊กที่ทำให้โปรแกรมพังได้** — เดิมตรวจอินพุตด้วย `.isdigit()` ซึ่งคืน `True` ให้ตัวยกอย่าง `"²"`
  แต่ `int("²")` โยน `ValueError` ออกมา เปลี่ยนมาใช้ `.isdecimal()` แทน
  ปิดช่องที่โปรแกรมจะพัง โดยเลขไทยอย่าง `"๑"` ยังใช้งานได้ตามปกติ
- ดัก `EOFError` ตอนรับอินพุต ทำให้โปรแกรมไม่พังเมื่อสตรีมอินพุตปิดลง (เช่นรันแบบ pipe)

### ที่มา
แยกโครงสร้างจากต้นฉบับเดิมที่รวมทุกอย่างไว้ในคลาส `FishingGame` ไฟล์เดียว (415 บรรทัด)
ซึ่งผูกตรรกะไว้กับ `input()` และ `print()` จนเขียน unit test ไม่ได้เลย
